apiVersion: apps/v1
kind: Deployment
metadata:
  name: javi-forecast
  namespace: apm
spec:
  template:
    spec:
      containers:
        - name: javi-forecast
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
