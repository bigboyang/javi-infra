apiVersion: apps/v1
kind: Deployment
metadata:
  name: javi-collector
  namespace: apm
spec:
  template:
    spec:
      containers:
        - name: collector
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
