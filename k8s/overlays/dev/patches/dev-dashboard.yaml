apiVersion: apps/v1
kind: Deployment
metadata:
  name: javi-dashboard
  namespace: apm
spec:
  template:
    spec:
      containers:
        - name: javi-dashboard
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
