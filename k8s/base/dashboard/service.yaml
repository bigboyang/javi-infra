apiVersion: v1
kind: Service
metadata:
  name: javi-dashboard
  namespace: apm
  labels:
    app: javi-dashboard
spec:
  selector:
    app: javi-dashboard
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
