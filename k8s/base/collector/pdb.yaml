apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: javi-collector
  namespace: apm
spec:
  # maxUnavailable: HPA scale에 따라 자동 조정 (minAvailable=1 + minReplicas=1 조합은 drain 무한대기 유발)
  maxUnavailable: 1
  selector:
    matchLabels:
      app: javi-collector
