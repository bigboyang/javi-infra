apiVersion: v1
kind: Service
metadata:
  name: javi-config-server
  namespace: apm
  labels:
    app: javi-config-server
spec:
  selector:
    app: javi-config-server
  ports:
    - name: http
      port: 18888
      targetPort: 18888
      protocol: TCP
  type: ClusterIP
