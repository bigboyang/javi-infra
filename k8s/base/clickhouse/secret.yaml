apiVersion: v1
kind: Secret
metadata:
  name: clickhouse-secret
  namespace: apm
type: Opaque
stringData:
  # 운영환경에서는 sealed-secrets 또는 external-secrets로 교체하세요.
  # 최소 16자 이상의 강력한 비밀번호를 설정하세요: openssl rand -base64 24
  password: "changeme-use-sealed-secrets"
